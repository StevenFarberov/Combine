package tests;

import static org.junit.Assert.*;

import java.awt.Point;
import java.util.Arrays;
import java.util.LinkedList;

import gamePlay.Game;
import gamePlay.PlayedBall;
import gamePlay.Util;
import gameState.Ball;
import gameState.Board;
import gameState.Board.Color;
import gameState.InputPair;
import gameState.InputPair.Orientation;

import org.junit.Before;
import org.junit.Test;


public class BoardTests {
	
	/*@Test
	public void testSimpleDrop() 
	{
		Board board = new Board();
		GameLogic gl = new GameLogic(board);
		LinkedList colors = new LinkedList<Color>();
		colors.add(Color.GREEN);
		colors.add(Color.YELLOW);
		
		try {
			gl.drop(0);
		} catch (Exception e) {
		}
		assertTrue(board.boardState[0][0] != null);
		if (pair.orietation == Orientation.HORIZONTAL)
		{
			assertTrue(board.boardState[1][0] != null);
		}
		if (pair.orietation == Orientation.VERTICAL)
		{
			assertTrue(board.boardState[0][1] != null);
		}
		assertTrue(board.boardState[0][2] == null);
	}
	
	@Test
	public void testMultiDrop() {
		Board board = new Board();
		GameLogic gl = new GameLogic(board);
		LinkedList colors = new LinkedList<Color>();
		colors.add(Color.GREEN);
		colors.add(Color.YELLOW);
		
		InputPair pair = new InputPair(colors);
		pair.orietation = Orientation.VERTICAL;
		try {
			gl.drop(pair, 0);
			gl.drop(pair, 0);
		} catch (Exception e) {
		}
		assertTrue(board.boardState[0][0] != null);
		assertTrue(board.boardState[0][1] != null);
		assertTrue(board.boardState[0][2] != null);
		assertTrue(board.boardState[0][3] != null);
		assertTrue(board.boardState[0][4] == null);
	}*/
	
	

}
